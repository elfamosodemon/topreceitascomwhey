const TopBar = () => (
  <div className="w-full py-3 px-4 text-center" style={{ background: 'hsl(20 90% 35%)' }}>
    <p className="text-primary-foreground font-semibold text-sm md:text-base animate-pulse-slow">
      ⚠️ Atenção: Essa promoção e os bônus exclusivos acabam hoje!
    </p>
  </div>
);

export default TopBar;
